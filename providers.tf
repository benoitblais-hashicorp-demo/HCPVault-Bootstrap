provider "vault" {
  # Configuration will be provided via environment variables:
  # VAULT_ADDR, VAULT_TOKEN, or VAULT_NAMESPACE
}

provider "tfe" {
  # Configuration will be provided via environment
}